# Docker profile configuration
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/docint
    username: docint
    password: docint_secret

minio:
  endpoint: http://minio:9000

rag-service:
  url: http://rag-service:8000

logging:
  level:
    com.docintel: INFO
    org.hibernate.SQL: WARN
